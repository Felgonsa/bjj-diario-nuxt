# Banco de dados PostgreSQL (Neon)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Ou variáveis individuais
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=bjj_diario
DB_SSL=false

# Para usar DATABASE_URL do Neon, copie a string de conexão do dashboard
# Exemplo: postgresql://user:pass@host.neon.tech/dbname?sslmode=require